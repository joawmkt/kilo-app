# Copiá este archivo a .env.local y completá con tus credenciales reales.
# .env.local NUNCA se sube al repo (ya está en .gitignore por defecto de Next.js).

# Supabase → Project Settings → API
SUPABASE_URL=https://TU-PROYECTO.supabase.co
SUPABASE_SERVICE_ROLE_KEY=TU_SERVICE_ROLE_KEY

# Esta es la única clave que SÍ puede quedar expuesta en el navegador
# (está protegida por Row Level Security). La usaremos cuando armemos
# el panel de login del carnicero.
NEXT_PUBLIC_SUPABASE_URL=https://TU-PROYECTO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=TU_ANON_KEY

# Twilio → Console → Account Info
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=TU_AUTH_TOKEN

# OpenAI → platform.openai.com/api-keys (se usa para transcribir los audios con Whisper)
OPENAI_API_KEY=TU_OPENAI_API_KEY
# Opcional: modelo de transcripción a usar. Por defecto "whisper-1".
# OPENAI_WHISPER_MODEL=whisper-1

# Anthropic → console.anthropic.com (se usa para interpretar el texto transcripto
# y convertirlo en una actualización de stock estructurada, con Claude Haiku)
ANTHROPIC_API_KEY=TU_ANTHROPIC_API_KEY
# Opcional: pisa el modelo de Claude Haiku por defecto. Conviene revisar de vez en
# cuando si hay una versión más nueva en https://platform.claude.com/docs/en/about-claude/models/overview
# CLAUDE_MODEL_HAIKU=claude-haiku-4-5-20251001